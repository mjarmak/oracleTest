public String getDecade(int age) {
    String decadeString = "";
 
    // This variable holds the maximum decade
    int decadeNumber = age / 10;
 
    // Add variable j
    int j = 0;
    for (int i = 0, j = 1; i < (decadeNumber + 1); i++, j++) {
        //
    }
    return decadeString;
}